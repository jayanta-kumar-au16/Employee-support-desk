spring:
  application:
    name: employee-support-spring-api
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 25MB

python-service:
  base-url: ${PYTHON_SERVICE_BASE_URL:http://localhost:8000}
  connect-timeout: 2s
  read-timeout: 5s
